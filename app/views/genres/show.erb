<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head><h1>Genres</h1>
  <h2><%=@genre.name%></h2>
  <body>
  <a href="/songs/<%@genre.songs.each do |s|%><%=s.slug%><%end%>"><p><%@genre.songs.each do |s|%><%=s.name%><%end%></p></a>

  <a href="/artists/<%@genre.artists.each do |a|%><%=a.slug%><%end%>">
      <p><%@genre.artists.each do |a|%><%=a.name%><%end%>
